# ⚡ دليل البدء السريع - Quick Start Guide

## 🎯 **5 خطوات فقط للاختبار:**

---

### **الخطوة 1: Supabase Setup** (5 دقائق)

#### **أ. شغّل Extended Schema:**
1. اذهب إلى: https://supabase.com/dashboard
2. افتح مشروعك → **SQL Editor** → **New Query**
3. انسخ محتوى: `web/supabase/extended-schema.sql`
4. الصقه في SQL Editor
5. اضغط **Run** (أو F5)
6. انتظر رسالة النجاح ✅

#### **ب. أضف بيانات تجريبية:**
1. في نفس SQL Editor → **New Query**
2. انسخ محتوى: `web/supabase/seed-data.sql`
3. الصقه وشغّله
4. يجب أن ترى: "Test data inserted successfully! 🎉"

#### **ج. أنشئ Storage Bucket:**
1. اذهب إلى: **Storage** (في القائمة الجانبية)
2. انقر: **Create a new bucket**
3. اسم الـ Bucket: `documents`
4. اختر: **Public bucket** ✅
5. انقر: **Create bucket**

---

### **الخطوة 2: تشغيل المشروع** (1 دقيقة)

```bash
# في Terminal:
cd web
npm run dev
```

انتظر حتى ترى:
```
✓ Ready in 3.2s
○ Local: http://localhost:3000
```

---

### **الخطوة 3: اختبار لوحة التحكم** (5 دقائق)

#### **افتح في المتصفح:**
```
http://localhost:3000/admin
```

#### **يجب أن ترى:**
- ✅ Sidebar على اليسار بالأزرق
- ✅ 4 بطاقات إحصائيات في الأعلى
- ✅ قسم "Recent Bookings" مع 5 حجوزات
- ✅ قسم "Quick Actions"

#### **جرّب:**
1. انقر **"Bookings"** في Sidebar
2. يجب أن ترى 10 حجوزات
3. اكتب "Ahmed" في شريط البحث
4. يجب أن يظهر حجز واحد فقط
5. انقر على 👁️ (View) لعرض التفاصيل
6. انقر ✅ لتأكيد حجز "pending"

#### **اختبار Messages:**
1. انقر **"Messages"** في Sidebar
2. يجب أن ترى 6 رسائل
3. انقر على 👁️ لقراءة رسالة
4. لاحظ تغير Status من "new" إلى "read"

---

### **الخطوة 4: اختبار البروفايل** (5 دقائق)

#### **افتح في المتصفح:**
```
http://localhost:3000/profile
```

#### **يجب أن ترى:**
- ✅ Sidebar على اليسار بالأبيض
- ✅ صورة المستخدم في الأعلى
- ✅ نموذج البيانات الشخصية
- ✅ 3 بطاقات إحصائيات في الأسفل

#### **جرّب:**
1. انقر **"Edit Profile"**
2. غيّر "Full Name" إلى اسم آخر
3. انقر **"Save"**
4. يجب أن يحفظ التغيير

#### **اختبار Bookings:**
1. انقر **"My Bookings"** في Sidebar
2. انقر Tab **"Upcoming"**
3. يجب أن ترى الحجوزات القادمة فقط
4. جرّب إلغاء حجز

#### **اختبار Documents:**
1. انقر **"My Documents"** في Sidebar
2. انقر **"Upload Document"**
3. اختر ملف PDF أو صورة
4. انتظر "Uploading..."
5. يجب أن يظهر في القائمة
6. جرّب حذف المستند

---

### **الخطوة 5: التحقق من البيانات** (2 دقيقة)

#### **في Supabase → Table Editor:**

افتح الجداول وتأكد من البيانات:

| جدول | العدد المتوقع |
|------|---------------|
| `bookings` | 10 |
| `contacts` | 6 |
| `user_profiles` | 3 |
| `notifications` | 7 |
| `documents` | 4 |

---

## ✅ **Checklist السريع:**

### **قاعدة البيانات:**
- [ ] Schema تم تشغيله بنجاح
- [ ] البيانات التجريبية تم إضافتها
- [ ] Storage bucket `documents` موجود
- [ ] الجداول تحتوي على البيانات

### **لوحة التحكم (`/admin`):**
- [ ] Dashboard تعرض الإحصائيات
- [ ] Bookings: يمكن البحث والتأكيد/الإلغاء
- [ ] Messages: يمكن القراءة والرد

### **البروفايل (`/profile`):**
- [ ] Profile: يمكن تعديل البيانات
- [ ] Bookings: تظهر وتفلتر حسب التاريخ
- [ ] Documents: يمكن رفع وحذف الملفات

---

## 🐛 **حل المشاكل السريع:**

### **مشكلة: "Cannot read property"**
```bash
# امسح cache وأعد التشغيل:
cd web
rm -rf .next
npm run dev
```

### **مشكلة: "Error fetching data"**
```bash
# تحقق من .env.local:
cat .env.local

# يجب أن يحتوي على:
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...
```

### **مشكلة: "Failed to upload file"**
- تأكد من إنشاء Storage bucket باسم `documents`
- اجعله Public
- أعد تشغيل المشروع

### **مشكلة: "Middleware redirect"**
افتح `middleware.ts` وغيّر السطر:
```typescript
const isAuthenticated = true; // مؤقتاً للاختبار
```

---

## 📊 **URLs للاختبار:**

| الصفحة | الرابط |
|--------|--------|
| الرئيسية | http://localhost:3000 |
| Admin Dashboard | http://localhost:3000/admin |
| Admin Bookings | http://localhost:3000/admin/bookings |
| Admin Messages | http://localhost:3000/admin/contacts |
| User Profile | http://localhost:3000/profile |
| User Bookings | http://localhost:3000/profile/bookings |
| User Documents | http://localhost:3000/profile/documents |

---

## 🎯 **النتيجة المتوقعة:**

إذا سارت كل الخطوات بنجاح:

✅ لوحة التحكم تعمل بالكامل
✅ يمكن إدارة الحجوزات والرسائل
✅ صفحة البروفايل تعمل بالكامل
✅ يمكن رفع وإدارة المستندات
✅ البيانات تحفظ في Supabase

---

## 📝 **بعد الاختبار:**

إذا نجح كل شيء، أخبرني وسأكمل:

1. ✅ صفحة إدارة المستخدمين (`/admin/users`)
2. ✅ نظام الإشعارات الكامل (Phase 4)
3. ✅ تحليل المستندات بالـ AI (Phase 5)
4. ✅ نظام التوصيات الذكية (Phase 5)

---

## ⚠️ **إذا واجهت مشكلة:**

أرسل لي:
1. Screenshot للخطأ
2. رسالة الخطأ من Console (اضغط F12)
3. الصفحة التي حدثت فيها المشكلة

---

**جاهز؟ ابدأ الآن! 🚀**

```bash
cd web && npm run dev
```
