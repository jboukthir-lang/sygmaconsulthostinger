# دليل اختبار الاتصال بقاعدة البيانات 🔍

## المشكلة الحالية ⚠️

رسالة الخطأ التي تظهر:
```
Access denied for user 'u611120010_sygma'@'localhost' (using password: YES)
```

**السبب:** لا توجد قاعدة بيانات MySQL محلياً على جهازك بنفس بيانات Hostinger.

**الحل:** اختبر الاتصال مباشرة على خادم Hostinger.

---

## الطريقة 1️⃣: اختبار PHP (الأسرع والأسهل) ⚡

### الخطوات:

1. **ارفع ملف الاختبار إلى Hostinger:**
   - ارفع الملف: `web/public/test-db.php`
   - المسار على الخادم: `/public_html/public/test-db.php`

2. **افتح المتصفح واذهب إلى:**
   ```
   https://sygmaconsult.com/test-db.php
   ```

3. **ستظهر لك صفحة تحتوي على:**
   - ✅ حالة الاتصال
   - ✅ قائمة الجداول الموجودة
   - ✅ إصدار MySQL
   - ✅ التحقق من الجداول المطلوبة

4. **⚠️ مهم جداً:** بعد الاختبار، احذف الملف فوراً:
   ```bash
   rm public/test-db.php
   ```

---

## الطريقة 2️⃣: اختبار Node.js

### الخطوات:

1. **ارفع الملف:**
   ```
   web/test-db-connection.js
   ```

2. **اتصل بـ SSH على Hostinger وشغل:**
   ```bash
   cd /home/u611120010/public_html
   node test-db-connection.js
   ```

3. **ستظهر لك النتائج في Terminal**

4. **بعد الاختبار احذف الملف:**
   ```bash
   rm test-db-connection.js
   ```

---

## الطريقة 3️⃣: استخدام API Route الموجود

الأسهل على الإطلاق:

1. **ارفع المشروع كاملاً على Hostinger**
2. **تأكد من إضافة متغيرات البيئة (Environment Variables)**
3. **افتح المتصفح:**
   ```
   https://sygmaconsult.com/api/health/db
   ```

4. **يجب أن ترى:**
   ```json
   {
     "status": "success",
     "message": "Database connection successful",
     "database": "u611120010_sygma",
     "host": "localhost"
   }
   ```

---

## حل المشاكل المحتملة 🔧

### مشكلة 1: "Access denied"

**الأسباب المحتملة:**
- ❌ كلمة المرور خاطئة
- ❌ اسم المستخدم خاطئ
- ❌ المستخدم ليس له صلاحيات على قاعدة البيانات

**الحل:**
1. اذهب إلى: **Hostinger → Databases → MySQL**
2. تحقق من:
   - اسم قاعدة البيانات: `u611120010_sygma`
   - اسم المستخدم: `u611120010_sygma`
   - كلمة المرور: `GZK446uj%`
3. إذا كانت خاطئة، **أعد تعيين كلمة المرور** وحدّث:
   - ملف `.env` على Hostinger
   - ملف `HOSTINGER_IMPORT.env` المحلي

---

### مشكلة 2: "Unknown database"

**السبب:** قاعدة البيانات غير موجودة

**الحل:**
1. اذهب إلى: **Hostinger → Databases**
2. أنشئ قاعدة بيانات جديدة باسم: `u611120010_sygma`
3. أنشئ مستخدم: `u611120010_sygma`
4. أعط المستخدم **جميع الصلاحيات** على القاعدة

---

### مشكلة 3: "Connection refused"

**السبب:** خدمة MySQL متوقفة أو غير متاحة

**الحل:**
1. تحقق من حالة خدمة MySQL في Hostinger
2. اتصل بدعم Hostinger الفني
3. تأكد من أنك تستخدم الخطة الصحيحة (Business أو أعلى)

---

### مشكلة 4: الجداول مفقودة

**السبب:** لم يتم استيراد ملفات SQL

**الحل:**
1. اذهب إلى: **Hostinger → phpMyAdmin**
2. اختر قاعدة البيانات: `u611120010_sygma`
3. اضغط على **Import/استيراد**
4. ارفع الملفات التالية بالترتيب:
   ```
   1. APPLY_THIS_SQL.sql
   2. FIX_BOOKINGS_FINAL.sql
   3. CREATE_CALENDAR_SYSTEM.sql
   4. APPLY_SITE_SETTINGS_SQL.sql
   ```

---

## التحقق من بيانات الاتصال في Hostinger 📋

### الخطوة 1: افتح phpMyAdmin

1. **Hostinger Dashboard**
2. **Websites → Your Website**
3. **Database → phpMyAdmin**

### الخطوة 2: تحقق من المعلومات

انسخ المعلومات التالية من Hostinger:

```
Database Name: __________________
Database User: __________________
Database Host: localhost (دائماً)
Database Port: 3306 (دائماً)
Password: __________________
```

### الخطوة 3: حدّث الملفات

إذا كانت المعلومات مختلفة، حدّث:

1. **ملف `.env` على Hostinger:**
   ```env
   DB_HOST=localhost
   DB_PORT=3306
   DB_USER=اسم_المستخدم_الصحيح
   DB_PASSWORD=كلمة_المرور_الصحيحة
   DB_NAME=اسم_القاعدة_الصحيح
   ```

2. **ملف `HOSTINGER_IMPORT.env` المحلي** (للمستقبل)

---

## الخطوات التالية بعد نجاح الاختبار ✅

1. ✅ **احذف ملفات الاختبار:**
   ```bash
   rm public/test-db.php
   rm test-db-connection.js
   ```

2. ✅ **ارفع المشروع كاملاً**

3. ✅ **اختبر الموقع:**
   - الصفحة الرئيسية: `https://sygmaconsult.com`
   - لوحة الإدارة: `https://sygmaconsult.com/admin`
   - API الصحة: `https://sygmaconsult.com/api/health/db`

4. ✅ **استيراد البيانات الأولية** (إذا لزم الأمر)

---

## ملفات مفيدة 📁

- ✅ `test-db.php` - اختبار PHP (صفحة ويب جميلة)
- ✅ `test-db-connection.js` - اختبار Node.js (Terminal)
- ✅ `HOSTINGER_IMPORT.env` - متغيرات البيئة للإنتاج
- ✅ `HOSTINGER_DEPLOYMENT_GUIDE_AR.md` - دليل الرفع الكامل

---

**تم التحديث:** 24 ديسمبر 2025
**الحالة:** جاهز للاختبار على Hostinger
