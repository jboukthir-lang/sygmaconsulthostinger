# 🧪 دليل الاختبار - Testing Guide

## 📋 **جدول المحتويات**
1. [الإعداد الأولي](#الإعداد-الأولي)
2. [اختبار قاعدة البيانات](#اختبار-قاعدة-البيانات)
3. [اختبار لوحة التحكم](#اختبار-لوحة-التحكم)
4. [اختبار صفحة البروفايل](#اختبار-صفحة-البروفايل)
5. [المشاكل الشائعة](#المشاكل-الشائعة)

---

## 🔧 **الإعداد الأولي**

### **الخطوة 1: تأكد من تثبيت المكتبات**
```bash
cd web
npm install
```

### **الخطوة 2: تحقق من ملف `.env.local`**
يجب أن يحتوي على:
```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### **الخطوة 3: تشغيل المشروع**
```bash
npm run dev
```

يجب أن يعمل على: `http://localhost:3000`

---

## 🗄️ **اختبار قاعدة البيانات**

### **1. تشغيل Schema الجديد**

#### **في Supabase Dashboard:**
1. اذهب إلى: **SQL Editor**
2. انقر: **New Query**
3. افتح ملف: `web/supabase/extended-schema.sql`
4. انسخ المحتوى بالكامل
5. الصقه في SQL Editor
6. اضغط: **Run**

#### **التحقق من نجاح التنفيذ:**
```sql
-- في SQL Editor، شغل هذا الاستعلام:
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public'
AND table_name IN (
    'notifications',
    'documents',
    'admin_users',
    'user_profiles',
    'recommendations',
    'activity_logs'
);
```

**النتيجة المتوقعة:** يجب أن يظهر 6 جداول.

---

### **2. إنشاء Storage Bucket للمستندات**

#### **في Supabase Dashboard:**
1. اذهب إلى: **Storage**
2. انقر: **Create a new bucket**
3. اسم الـ Bucket: `documents`
4. اختر: **Public bucket** (اختياري)
5. انقر: **Create bucket**

#### **إعداد RLS للـ Storage:**
```sql
-- في SQL Editor:
-- Allow authenticated users to upload their documents
CREATE POLICY "Users can upload their own documents"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (
  bucket_id = 'documents'
  AND (storage.foldername(name))[1] = auth.uid()::text
);

-- Allow users to read their own documents
CREATE POLICY "Users can read their own documents"
ON storage.objects FOR SELECT
TO authenticated
USING (
  bucket_id = 'documents'
  AND (storage.foldername(name))[1] = auth.uid()::text
);
```

---

### **3. إضافة بيانات تجريبية**

#### **أ. إضافة حجوزات تجريبية:**
```sql
INSERT INTO bookings (name, email, topic, date, time, status)
VALUES
  ('Ahmed Hassan', 'ahmed@test.com', 'Strategic Consulting', '2025-12-20', '10:00', 'pending'),
  ('Sarah Martin', 'sarah@test.com', 'Legal & Fiscal', '2025-12-22', '14:00', 'confirmed'),
  ('Mohamed Ali', 'mohamed@test.com', 'Company Formation', '2025-12-18', '09:00', 'confirmed'),
  ('Emma Dubois', 'emma@test.com', 'Digital Transformation', '2025-12-15', '11:00', 'cancelled');
```

#### **ب. إضافة رسائل تجريبية:**
```sql
INSERT INTO contacts (name, email, subject, message, status)
VALUES
  ('John Smith', 'john@test.com', 'General Inquiry', 'I would like to know more about your visa services.', 'new'),
  ('Marie Claire', 'marie@test.com', 'Partnership', 'Interested in potential partnership opportunities.', 'new'),
  ('Ali Ben Ahmed', 'ali@test.com', 'Careers', 'Looking for job opportunities at Sygma Consult.', 'read');
```

#### **ج. إضافة ملف شخصي تجريبي:**
```sql
INSERT INTO user_profiles (user_id, email, full_name, phone, company, country, language)
VALUES
  ('demo-user-id', 'demo@email.com', 'Demo User', '+33 6 12 34 56 78', 'Tech Corp', 'france', 'fr');
```

#### **د. إضافة إشعارات تجريبية:**
```sql
INSERT INTO notifications (user_id, title, message, type, read)
VALUES
  ('demo-user-id', 'Booking Confirmed', 'Your consultation on Dec 20 has been confirmed.', 'booking', false),
  ('demo-user-id', 'Reminder', 'You have a meeting tomorrow at 10:00 AM.', 'reminder', false),
  ('demo-user-id', 'Document Analyzed', 'Your uploaded document has been analyzed.', 'system', true);
```

---

## 🎛️ **اختبار لوحة التحكم**

### **الوصول للوحة التحكم:**
```
URL: http://localhost:3000/admin
```

**ملاحظة:** حالياً، لا يوجد نظام مصادقة، لذا يمكنك الوصول مباشرة.

---

### **1. اختبار Dashboard الرئيسية** (`/admin`)

#### **✅ يجب أن ترى:**
- [ ] 4 بطاقات إحصائيات:
  - Total Bookings (العدد من قاعدة البيانات)
  - New Messages
  - Registered Users
  - Conversion Rate (64%)

- [ ] قسم "Recent Bookings" مع آخر 5 حجوزات
- [ ] قسم "Quick Actions" مع 3 أزرار
- [ ] قسم "Monthly Overview" (placeholder)

#### **🧪 الاختبارات:**
1. تحقق من أن الأرقام صحيحة
2. انقر على "Confirm Pending Bookings" (لا شيء يحدث - عادي)
3. تحقق من تحديث الصفحة تلقائياً

---

### **2. اختبار إدارة الحجوزات** (`/admin/bookings`)

#### **✅ يجب أن ترى:**
- [ ] 3 بطاقات إحصائيات (Pending, Confirmed, Cancelled)
- [ ] جدول بكل الحجوزات
- [ ] شريط بحث في الأعلى
- [ ] أيقونات للإجراءات (View, Confirm, Cancel)

#### **🧪 الاختبارات:**

**أ. اختبار البحث:**
1. اكتب في شريط البحث: "Ahmed"
2. يجب أن يظهر فقط الحجز الخاص بـ Ahmed
3. امسح البحث

**ب. اختبار عرض التفاصيل:**
1. انقر على أيقونة العين 👁️ لأي حجز
2. يجب أن يفتح Modal بالتفاصيل
3. انقر خارج الـ Modal للإغلاق

**ج. اختبار التأكيد:**
1. ابحث عن حجز بحالة "pending"
2. انقر على أيقونة التأكيد ✅
3. يجب أن يتغير status إلى "confirmed"
4. الصفحة تتحدث تلقائياً

**د. اختبار الإلغاء:**
1. اختر حجز "pending"
2. انقر على أيقونة الإلغاء ❌
3. يجب أن يتغير status إلى "cancelled"

**هـ. اختبار الترتيب:**
1. انقر على header "Name"
2. يجب أن يرتب الأسماء أبجدياً
3. انقر مرة أخرى - ترتيب عكسي

**و. اختبار Pagination:**
1. إذا كان لديك أكثر من 10 حجوزات
2. يجب أن ترى أزرار التنقل في الأسفل
3. انقر "Next" - صفحة جديدة

---

### **3. اختبار إدارة الرسائل** (`/admin/contacts`)

#### **✅ يجب أن ترى:**
- [ ] عداد "X New" في الأعلى
- [ ] جدول بكل الرسائل
- [ ] Status badges بألوان مختلفة

#### **🧪 الاختبارات:**

**أ. اختبار عرض الرسالة:**
1. انقر على أيقونة العين 👁️
2. يجب أن يفتح Modal بمحتوى الرسالة
3. تحقق من أن الـ status تغير من "new" إلى "read"

**ب. اختبار الرد:**
1. افتح رسالة
2. انقر "Reply via Email"
3. يجب أن يفتح برنامج البريد الإلكتروني

**ج. اختبار تحديد كـ مقروءة:**
1. ابحث عن رسالة "new"
2. انقر على أيقونة ✓✓
3. يجب أن يتغير status إلى "read"

---

## 👤 **اختبار صفحة البروفايل**

### **الوصول لصفحة البروفايل:**
```
URL: http://localhost:3000/profile
```

---

### **1. اختبار الصفحة الرئيسية** (`/profile`)

#### **✅ يجب أن ترى:**
- [ ] Header مع صورة المستخدم
- [ ] زر "Edit Profile"
- [ ] نموذج بالبيانات الشخصية (معطل)
- [ ] 3 بطاقات إحصائيات في الأسفل

#### **🧪 الاختبارات:**

**أ. اختبار التعديل:**
1. انقر "Edit Profile"
2. الحقول تصبح قابلة للتعديل
3. الأزرار تتغير إلى "Cancel" و "Save"

**ب. اختبار الحفظ:**
1. عدّل أي حقل (مثلاً: Full Name)
2. انقر "Save"
3. يظهر "Saving..." ثم يختفي
4. الحقول تصبح معطلة مرة أخرى

**ج. اختبار الإلغاء:**
1. انقر "Edit Profile"
2. عدّل أي حقل
3. انقر "Cancel"
4. يجب أن تعود البيانات للأصل

**د. اختبار اللغة:**
1. غيّر "Preferred Language"
2. احفظ
3. (حالياً لن يتغير UI - سيتم تطبيقه لاحقاً)

---

### **2. اختبار صفحة الحجوزات** (`/profile/bookings`)

#### **✅ يجب أن ترى:**
- [ ] 3 تبويبات (All, Upcoming, Past)
- [ ] زر "New Booking"
- [ ] قائمة الحجوزات

#### **🧪 الاختبارات:**

**أ. اختبار الفلترة:**
1. انقر "All" - يظهر كل الحجوزات
2. انقر "Upcoming" - يظهر الحجوزات القادمة فقط
3. انقر "Past" - يظهر الحجوزات الماضية

**ب. اختبار الإلغاء:**
1. ابحث عن حجز "confirmed" قادم
2. انقر زر "Cancel"
3. تأكيد في popup
4. يجب أن يتغير status إلى "cancelled"

**ج. اختبار Google Meet:**
1. إذا كان لديك حجز مع `meet_link`
2. يجب أن ترى رابط "Join Meeting"
3. انقر عليه - يفتح في تبويب جديد

**د. اختبار حجز جديد:**
1. انقر "New Booking"
2. يجب أن يحولك لصفحة `/book`

---

### **3. اختبار صفحة المستندات** (`/profile/documents`)

#### **✅ يجب أن ترى:**
- [ ] زر "Upload Document"
- [ ] رسالة معلومات (Info card)
- [ ] إذا لا توجد مستندات: رسالة "No documents"
- [ ] إذا توجد: Grid بالمستندات

#### **🧪 الاختبارات:**

**أ. اختبار الرفع:**
1. انقر "Upload Document"
2. اختر ملف (PDF, JPG, PNG, DOC)
3. يظهر "Uploading..."
4. بعد الانتهاء، يظهر المستند في القائمة

⚠️ **ملاحظة مهمة:**
إذا فشل الرفع، تأكد من:
- إنشاء Storage bucket باسم `documents` في Supabase
- تفعيل RLS policies للـ Storage

**ب. اختبار عرض المستند:**
1. انقر على أيقونة العين 👁️
2. (حالياً لا شيء يحدث - سيتم تطبيقه لاحقاً)

**ج. اختبار التحميل:**
1. انقر على أيقونة التحميل ⬇️
2. (حالياً لا شيء يحدث - سيتم تطبيقه لاحقاً)

**د. اختبار الحذف:**
1. انقر على أيقونة سلة المهملات 🗑️
2. تأكيد في popup
3. يجب أن يختفي المستند من القائمة

---

## 🐛 **المشاكل الشائعة وحلولها**

### **مشكلة 1: "Error fetching data"**

**السبب:**
- Supabase غير متصل
- خطأ في `.env.local`

**الحل:**
```bash
# تحقق من ملف .env.local
cat .env.local

# يجب أن يحتوي على:
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
```

---

### **مشكلة 2: "Middleware redirect loop"**

**السبب:**
- الـ middleware يحول جميع الطلبات للصفحة الرئيسية

**الحل المؤقت:**
في `middleware.ts`، علّق السطر:
```typescript
// Temporarily disable auth check for testing
const isAuthenticated = true; // Always true for now
```

---

### **مشكلة 3: "Failed to upload file"**

**السبب:**
- Storage bucket غير موجود
- RLS policies خاطئة

**الحل:**
1. تحقق من وجود bucket `documents` في Supabase Storage
2. شغّل RLS policies المذكورة أعلاه
3. أعد تشغيل المشروع

---

### **مشكلة 4: "Loading forever / White screen"**

**السبب:**
- خطأ JavaScript في Console
- مكون معطل

**الحل:**
1. افتح Browser DevTools (F12)
2. اذهب لـ Console
3. ابحث عن أخطاء حمراء
4. أرسل لي الخطأ

---

### **مشكلة 5: "Bookings not showing"**

**السبب:**
- لا توجد بيانات في قاعدة البيانات
- خطأ في الاستعلام

**الحل:**
```sql
-- في Supabase SQL Editor، تحقق من البيانات:
SELECT * FROM bookings;

-- إذا كانت فارغة، أضف بيانات تجريبية (راجع القسم أعلاه)
```

---

## ✅ **Checklist الاختبار الشامل**

### **البنية التحتية:**
- [ ] المشروع يعمل على `localhost:3000`
- [ ] لا توجد أخطاء في Terminal
- [ ] لا توجد أخطاء في Browser Console
- [ ] Supabase متصل بنجاح

### **قاعدة البيانات:**
- [ ] Schema تم تشغيله بنجاح
- [ ] الـ 6 جداول موجودة
- [ ] Storage bucket `documents` موجود
- [ ] بيانات تجريبية تم إضافتها

### **لوحة التحكم:**
- [ ] Dashboard تعرض الإحصائيات
- [ ] Bookings تعرض القائمة
- [ ] يمكن تأكيد/إلغاء الحجوزات
- [ ] Messages تعرض الرسائل
- [ ] يمكن قراءة الرسائل
- [ ] البحث والفلترة يعملان

### **صفحة البروفايل:**
- [ ] Profile يعرض البيانات
- [ ] يمكن تعديل البيانات
- [ ] Bookings تعرض الحجوزات
- [ ] التبويبات تعمل (All, Upcoming, Past)
- [ ] Documents تعرض المستندات
- [ ] يمكن رفع مستند جديد
- [ ] يمكن حذف مستند

---

## 📊 **تقرير الاختبار**

بعد الانتهاء من الاختبار، يرجى ملء هذا التقرير:

```
التاريخ: _______________
المتصفح: _______________

✅ ما يعمل:
-
-
-

❌ ما لا يعمل:
-
-
-

🐛 الأخطاء:
-
-
-

💡 اقتراحات:
-
-
-
```

---

## 🚀 **الخطوات التالية**

بعد نجاح الاختبار:
1. إكمال صفحة إدارة المستخدمين (`/admin/users`)
2. بناء نظام الإشعارات (Phase 4)
3. تطبيق تحليل المستندات بالـ AI (Phase 5)
4. ربط نظام المصادقة الحقيقي (Firebase Auth)

---

**أي مشكلة أو سؤال؟ أخبرني! 🎯**
